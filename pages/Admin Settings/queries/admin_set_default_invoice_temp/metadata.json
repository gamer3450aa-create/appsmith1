{
  "gitSyncId": "6a18d17583dcde86e3939d70_056372a3-92d1-4bae-98f8-f85228a01d0b",
  "id": "Admin Settings_admin_set_default_invoice_temp",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "WITH current_operator AS (\n    SELECT require_app_user_id_by_email({{ appsmith.user.email }}) AS user_id\n), selected_template AS (\n    SELECT id, branch_id, invoice_type, paper_size\n    FROM invoice_templates\n    WHERE id = {{ TableInvoiceTemplates.selectedRow.id }}::uuid\n), cleared_defaults AS (\n    UPDATE invoice_templates it\n    SET is_default = false\n    FROM selected_template st, current_operator\n    WHERE it.invoice_type = st.invoice_type\n      AND it.paper_size = st.paper_size\n      AND (\n          it.branch_id = st.branch_id\n          OR (it.branch_id IS NULL AND st.branch_id IS NULL)\n      )\n    RETURNING it.id\n)\nUPDATE invoice_templates it\nSET is_default = true,\n    is_active = true\nFROM selected_template st, current_operator\nWHERE it.id = st.id\nRETURNING it.*;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "ERP_PostgreSQL",
      "isAutoGenerated": false,
      "name": "ERP_PostgreSQL",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "admin_set_default_invoice_temp",
    "pageId": "Admin Settings",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}