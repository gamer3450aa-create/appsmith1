WITH current_operator AS (
    SELECT require_app_user_id_by_email({{ appsmith.user.email }}) AS user_id
)
UPDATE users u
SET is_active = false
FROM current_operator
WHERE u.id = {{ TableUsers.selectedRow.id }}::uuid
RETURNING u.id, u.full_name, u.email, u.is_active;