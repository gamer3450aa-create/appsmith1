WITH current_operator AS (
    SELECT require_app_user_id_by_email({{ appsmith.user.email }}) AS user_id
)
INSERT INTO car_brands (name, country, is_active)
SELECT
    {{ InputCarBrandName.text }},
    NULLIF({{ InputCarBrandCountry.text }}, ''),
    COALESCE({{ SwitchCarBrandActive.isSwitchedOn }}, true)
FROM current_operator
RETURNING *;