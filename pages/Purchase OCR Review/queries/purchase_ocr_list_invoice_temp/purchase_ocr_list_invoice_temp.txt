SELECT id AS value, template_name AS label, template_code, invoice_type, paper_size
FROM invoice_templates
WHERE is_active = true
  AND invoice_type = 'purchase_invoice'
ORDER BY is_default DESC, template_name;